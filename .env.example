# Lokal: nach .env.local kopieren. Nicht NEXT_PUBLIC_ — der Key wird nur serverseitig
# gelesen und bei Bedarf über /api/google-maps-key an den Client ausgegeben (kein Inlining im Bundle).
GOOGLE_MAPS_API_KEY=
